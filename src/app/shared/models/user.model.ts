export default interface UserModelRegister {
  email: string;
  password: string;
  firstName: string;
  lastName: string;
}

export default interface UserModelLogin {
  email: string;
  password: string;
}
